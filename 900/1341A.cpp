#include <bits/stdc++.h>
#define LOG(x) cout << x << "\n"
// unsolved
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    int a, b, c, d;
    cin >> n >> a >> b >> c >> d;
    for (int i = b + a; i >= b - a; i++) {
        
    }
  }
  return 0;
}