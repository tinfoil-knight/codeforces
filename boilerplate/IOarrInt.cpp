#include <bits/stdc++.h>
#define LOG(x) cout << x << "\n"

using namespace std;

int main() {
  int n, m;
  vector<int> arr;

  cin >> n;

  while (n--) {
    cin >> m;
    arr.push_back(m);
  }

  // Code goes here
  
  return 0;
}